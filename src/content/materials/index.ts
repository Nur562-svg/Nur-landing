import { assertValidMaterialCatalog } from "@/lib/material-validation";
import { materialCatalog } from "./material-catalog";

assertValidMaterialCatalog(materialCatalog);

export { materialCatalog };
